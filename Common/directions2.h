#pragma once

vec3 const RIGHT	= vec3( 1.0f, 0.0f, 0.0f);
vec3 const AHEAD	= vec3( 0.0f, 0.0f, 1.0f);
vec3 const LEFT		= vec3(-1.0f, 0.0f, 0.0f);
vec3 const BACK		= vec3( 0.0f, 0.0f,-1.0f);
vec3 const UP		= vec3( 0.0f, 1.0f, 0.0f);
vec3 const DOWN		= vec3( 0.0f,-1.0f, 1.0f);
